'use client';
import MenuPage from '@/components/MenuPage';

export default function MenuSection() {
    return (
        <section id="menu" className="relative bg-[#0a0a0a] overflow-hidden w-full">
            <MenuPage />
        </section>
    );
}
